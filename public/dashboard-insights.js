var dashboardBeforeInsights=dashboard;
function evolutionChart(){const sessions=db.questionSessions.slice().sort((a,b)=>a.date.localeCompare(b.date)).slice(-12);if(!sessions.length)return'<div class="empty">Registre questões para visualizar sua evolução.</div>';const w=680,h=205,pad=28,step=sessions.length>1?(w-pad*2)/(sessions.length-1):0,apparentPoints=sessions.map((x,i)=>`${pad+i*step},${h-pad-(apparent(x)/100)*(h-pad*2)}`).join(' '),realPoints=sessions.map((x,i)=>`${pad+i*step},${h-pad-(real(x)/100)*(h-pad*2)}`).join(' ');return `<div class="evolution-chart"><svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Evolução do desempenho aparente e real"><defs><linearGradient id="areaGreen" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#20a776" stop-opacity=".18"/><stop offset="1" stop-color="#20a776" stop-opacity="0"/></linearGradient></defs>${[25,50,75,100].map(v=>`<g><line x1="${pad}" y1="${h-pad-(v/100)*(h-pad*2)}" x2="${w-pad}" y2="${h-pad-(v/100)*(h-pad*2)}"/><text x="1" y="${h-pad-(v/100)*(h-pad*2)+3}">${v}%</text></g>`).join('')}<polygon class="evolution-area" points="${pad},${h-pad} ${realPoints} ${pad+(sessions.length-1)*step},${h-pad}"/><polyline class="line-apparent" points="${apparentPoints}"/><polyline class="line-real" points="${realPoints}"/>${sessions.map((x,i)=>`<circle class="point-real" cx="${pad+i*step}" cy="${h-pad-(real(x)/100)*(h-pad*2)}" r="3.5"><title>${x.date.split('-').reverse().join('/')} · Real ${real(x)}%</title></circle>`).join('')}</svg><div class="evolution-labels">${sessions.map((x,i)=>i===0||i===sessions.length-1||i%3===0?`<span style="left:${sessions.length>1?i/(sessions.length-1)*100:50}%">${x.date.slice(5).replace('-','/')}</span>`:'').join('')}</div></div>`}
function confidenceData(){const rawTotal=db.questionSessions.reduce((n,x)=>n+(Number(x.total)||0),0),rawSkipped=db.questionSessions.reduce((n,x)=>n+(Number(x.skipped)||0),0),skipped=Math.min(rawTotal,rawSkipped),answered=Math.max(0,rawTotal-skipped),rawGuessed=db.questionSessions.reduce((n,x)=>n+(Number(x.guessed)||0),0),guessed=Math.min(answered,rawGuessed),rawDoubted=db.questionSessions.reduce((n,x)=>n+(Number(x.doubted)||0),0),doubted=Math.min(answered-guessed,rawDoubted),sure=Math.max(0,answered-guessed-doubted);return{total:answered,rawTotal,guessed,doubted,skipped,sure}}
function dashboardInsights(){const confidence=confidenceData(),pct=n=>confidence.total?Math.round(n/confidence.total*100):0,exams=db.exams,examTotal=exams.reduce((n,x)=>n+(Number(x.total)||0),0),examCorrect=exams.reduce((n,x)=>n+(Number(x.correct)||0),0),examAvg=exams.length?Math.round(exams.reduce((n,x)=>n+apparent(x),0)/exams.length):0,examReal=exams.length?Math.round(exams.reduce((n,x)=>n+real({...x,difficulty:'medio'}),0)/exams.length):0,best=exams.length?exams.reduce((a,b)=>apparent(a)>apparent(b)?a:b):null,worst=exams.length?exams.reduce((a,b)=>apparent(a)<apparent(b)?a:b):null,weak=grouped().filter(x=>x.real<db.settings.cutoff).length,totalWrong=db.questionSessions.reduce((n,x)=>n+Math.max(0,(Number(x.total)||0)-(Number(x.correct)||0)),0);const enemies=[['Erros',totalWrong,'#db5c62'],['Dúvidas',confidence.doubted,'#e6a23c'],['Chutes',confidence.guessed,'#d98345'],['Questões puladas',confidence.skipped,'#8d91a0'],['Tópicos abaixo do corte',weak,'#b45765']].sort((a,b)=>b[1]-a[1]),maxEnemy=Math.max(1,...enemies.map(x=>x[1]));return `<section class="dashboard-insights"><div class="insight-section-head"><div><span class="eyebrow-chip">ANÁLISE GERAL</span><h2>Seu desempenho em detalhes</h2><p>Leitura consolidada das suas questões, provas e simulados.</p></div><div class="semantic-legend"><span><i class="success"></i>Acerto</span><span><i class="warning"></i>Atenção</span><span><i class="danger"></i>Erro</span></div></div><div class="insights-grid"><article class="card evolution-card span-2"><div class="card-head"><div><h2>Evolução de desempenho</h2><p class="head-copy">Últimas ${Math.min(12,db.questionSessions.length)} sessões registradas</p></div><div class="chart-legend"><span><i class="legend-apparent"></i>Aparente</span><span><i class="success"></i>Real</span></div></div>${evolutionChart()}</article><article class="card confidence-card"><div class="card-head"><div><h2>Confiança geral</h2><p class="head-copy">Como você respondeu</p></div></div><div class="confidence-donut" style="--sure:${pct(confidence.sure)};--doubt:${pct(confidence.doubted)}"><div><b>${pct(confidence.sure)}%</b><span>certeza</span></div></div><div class="confidence-list"><div><i class="success"></i><span>Certeza</span><b>${confidence.sure}</b><small>${pct(confidence.sure)}%</small></div><div><i class="warning"></i><span>Dúvida</span><b>${confidence.doubted}</b><small>${pct(confidence.doubted)}%</small></div><div><i class="danger"></i><span>Chute</span><b>${confidence.guessed}</b><small>${pct(confidence.guessed)}%</small></div></div></article><article class="card exam-details"><div class="card-head"><div><h2>Detalhes de simulados</h2><p class="head-copy">Resumo de todas as provas</p></div><span class="badge good">${exams.length} realizadas</span></div><div class="detail-list"><div><span>Total de questões</span><b>${fmt(examTotal)}</b></div><div><span>Total de acertos</span><b class="text-success">${fmt(examCorrect)}</b></div><div><span>Média aparente</span><b>${examAvg}%</b></div><div><span>Média real</span><b>${examReal}%</b></div><div><span>Melhor resultado</span><b class="text-success">${best?`${escapeHtml(best.name)} · ${apparent(best)}%`:'—'}</b></div><div><span>Ponto de atenção</span><b class="text-danger">${worst?`${escapeHtml(worst.name)} · ${apparent(worst)}%`:'—'}</b></div></div></article><article class="card enemies-card"><div class="card-head"><div><h2>Maiores inimigos</h2><p class="head-copy">Fatores que mais reduzem seu resultado geral</p></div><span class="badge">GERAL</span></div><div class="enemy-list">${enemies.map(([name,value,color],i)=>`<div><div><span><b>${i+1}</b>${name}</span><strong>${value}</strong></div><div class="enemy-track"><i style="width:${value/maxEnemy*100}%;background:${color}"></i></div></div>`).join('')}</div><p class="enemy-tip">Priorize o primeiro item da lista no seu próximo ciclo de revisão.</p></article></div></section>`}
dashboard=function(){const html=dashboardBeforeInsights(),marker='<section class="card achievement-widget">',index=html.indexOf(marker);return index<0?html+dashboardInsights():html.slice(0,index)+dashboardInsights()+html.slice(index)};
